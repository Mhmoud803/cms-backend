---
alwaysApply: true
---
# CMS Backend Rules

### Interaction protocol
1) First response to any user message:
- Ask clarifying questions needed to fully understand the task.
- Number of questions depends on complexity; ask only what is necessary.
- Do not take any action (no edits, commands, or irreversible steps) until answers are received.

2) After clarifications are provided:
- Convert the request and clarifications into a single JSON object using the schema below.
- Display the JSON to the user before acting, in a fenced json code block.
- Then act on the task, using the JSON as the source of truth. If anything remains ambiguous, ask a brief follow-up before acting.

3) Ongoing:
- If new information arrives, update and redisplay the JSON before any further action.

### JSON schema
Use exactly this shape when converting inputs:
```json
{
  "task": "",
  "context": "",
  "requirements": [],
  "constraints": [],
  "inputs": [],
  "assumptions": [],
  "deliverables": [],
  "acceptance_criteria": [],
  "priority": "low|medium|high",
  "risk_flags": []
}
```

### Rules
- Always display the JSON version of the user's input before acting and act on that
- Always wait for answers to clarifying questions before taking any action.
- **NEVER commit changes to Git** - user handles all commits manually
- **After finishing each task, update the docs if relevant** - see the Documentation section
- Keep language direct and clear.
- Keep questions proportionate to complexity.

## Project Overview
- **Mission**: Build platform for Quranic data distribution
- **Two main actors**: Publishers (upload data) and Consumers/Developers (access data)
- **Non-profit**: Community-first, open-source approach

### Branch Strategy & Protection Rules
- **main**: Production-ready code, **MERGE-ONLY** from staging
- **staging**: Primary development branch for new features and bug fixes

### Enforced Git Workflow Rules
**CRITICAL**: These rules are strictly enforced and cannot be bypassed:

1. **Branch Protection**:
   - **main** and **staging** branches: NO direct commits/pushes allowed
   - **feat/** branches: Direct commits allowed for feature development

2. **Mandatory Change Flow**:
   - ALL changes MUST start on `staging` branch (or feature branch created from staging)
   - Changes MUST be tested locally BEFORE any remote push
   - Changes MUST commit with descriptive but concise messages
   - Changes MUST push to `feat/*` after local testing passes

3. **Branch Transition Rules**:
   - `staging` → `main`: ONLY via Pull Request + code review
   - `feat/*` → `staging`: Via Pull Request or direct merge after testing

### Merge Order Rule (MANDATORY)
- **Sequence**: `staging` MUST be merged into `main`.
- **Prohibited**: Skipping stages or merging directly into `staging` or `main` from any other branch is not allowed.


4. **CMS Rules File Updates**:
   - When CMS rules are modified, automatic commit with message "Update CMS workflow rules"
   - Must test locally immediately after rules change
   - Must push to staging origin once local testing confirms functionality


### Deployment Rules (ENFORCED)
**Strict branch flow enforcement via GitHub Actions:**
1. **main** can ONLY be updated from **staging** via Pull Request
2. **staging** can ONLY be updated from feature branches via Pull Request
3. Deployment triggers ONLY on successful PR merges (not direct pushes)


## Environments & URLs
- Production: `https://cms.itqan.dev`
- Staging: `https://staging.cms.itqan.dev`

## Dependencies docs
- ninja docs: https://django-ninja.dev/
- django docs: https://docs.djangoproject.com/en/5.2/
- django-allauth docs: https://docs.allauth.org/en/dev/ is used for handling social auth and OAuth2


## Raising errors
- If you want to return an error to the user especially if it is 400 error use `apps.core.ninja_utils.errors.ItqanError`
- If you want to raise 403 Error, use `from rest_framework.exceptions.PermissionDenied`
- If an endpoint view may raise an error, document it with `apps.core.ninja_utils.errors.NinjaErrorResponse` as in
```python
response={
        200: OkSchema,
        400: NinjaErrorResponse[Literal["activation_request_failed"]],
        404: NinjaErrorResponse[Literal["user_not_found"]]
        | NinjaErrorResponse[Literal["admin_not_found"]],
    }
```
- The `message` argument passed to `ItqanError` **must always be localized** using `gettext_lazy` (imported as `_` from `django.utils.translation`). Example:
```python
from django.utils.translation import gettext_lazy as _

raise ItqanError(
    error_name="asset_not_found",
    message=_("Asset does not exist."),
    status_code=404,
)
```
- After adding any new translatable string (any call to `_(...)`), you **must** run `makemessages` as specified in the Actions section, then add the Arabic translation in `locale/ar/LC_MESSAGES/django.po`.

## Documentation
As the **last step of every task**, check whether the change made any documentation stale and update it in the same change set. Do not defer this to a follow-up task.

### Where the docs live
- `docs/ARCHITECTURE.md` — domain models, the ER diagram, API surfaces, access control and content lifecycle. Update when you add/remove/rename a model or field, change how authorization works, or change what an API surface is for.
- `docs/AUTHENTICATION.md` — auth flows (allauth, OAuth2, JWT, SAML) and their error codes. Update when you touch login, tokens, scopes, or auth-related error responses.
- `README.md` / `CONTRIBUTING.md` — setup, dependencies, commands, workflow. Update when you change how the project is installed, configured, or run.
- `docs-website/docs/` — the **public developer API** site (Docusaurus). Only covers `developers_api`. Update when you change a public endpoint's request/response shape, pagination, filtering, or error contract. **Portal/tenant/internal-only changes do not belong here.**

### When it is relevant
Update the docs if the change does any of:
- adds, removes, or renames a model, field, or relationship that the docs describe
- changes an API contract (request/response fields, status codes, `error_name` values)
- changes authorization: permissions, groups, or who may perform an action
- changes a documented workflow, command, or environment variable
- makes an existing statement in the docs factually wrong

Skip the docs only when the change is genuinely invisible to them — an internal refactor, a test-only change, or a bugfix that restores already-documented behaviour.

### How
- Fix **surrounding** stale statements you notice while editing, not just the line your change touched. Docs drift; leaving a known-wrong neighbouring line is not acceptable.
- Keep the existing structure, tone, and formatting of the file. Do not restructure a doc as a side effect of a small change.
- Keep mermaid diagrams in sync with the models they depict.
- Say plainly in your final summary which docs you updated, or that none were relevant and why.

## Actions:
- migrate: uv run manage.py migrate
- makemigrations: uv run makemigrations
- makemessages: uv run manage.py extendedmakemessages --no-location --no-wrap --locale=ar --no-fuzzy-matching --keep-header

## Tests
- All tests should be written as AAA Arrange, Act, Assert explicitly as comments in test cases
- All tests should be writen test_<function_name>_where<criteria>_should<expected_results>
- When using self.authenticate_user in tests, put it into the test case itself not in `setUp` or `setUpClass` methods to be more explicit
- Use `pytest` to run test NOT `python manage.py test`
- If you are testing an API endpoint, do not just test the status code for 4xx responses, you have to asset the `error_name` as well
- When you try to run the runserver, migrations, or tests, try to use virtual env at .venv first.
- When patching, do not use hardcoded dotted-path strings. Import the real symbol and use `patch.object` with `<symbol>.__name__` so renames propagate automatically and refactoring stays safe. Patch the name on the module where it is *imported/called* (the call site), not on its defining module. Example:
```python
from apps.content.api.portal import recitation_tracks_upload
from apps.content.api.portal.recitation_tracks_upload import (
    AssetRecitationAudioTracksDirectUploadService,
    sync_asset_recitations_json_file,
)

with patch.object(
    AssetRecitationAudioTracksDirectUploadService,
    AssetRecitationAudioTracksDirectUploadService.finish_upload.__name__,
    return_value=...,
), patch.object(
    recitation_tracks_upload,
    sync_asset_recitations_json_file.__name__,
) as sync_json:
    ...
```

## API Architecture
This project exposes **four distinct APIs**, each serving a different audience. All are built with Django Ninja and share the same error handling structure (`ItqanError` / `NinjaErrorResponse`).

- **cms_api** (Internal API): Mounted at `/cms-api/`. Powers the frontend SPA where platform users ("the developers") can create accounts, explore the platform, and create OAuth applications. Uses JWT/Session authentication via django-allauth.
- **developers_api** (Public API): Mounted at `/` (root). The public-facing API consumed by external developers after they create an OAuth application through the CMS API. This is where we expect the majority of traffic. Uses OAuth2 via django-oauth-toolkit.
- **tenant_api** (Tenant API): Mounted at `/tenant/`. A multi-tenant SaaS API for **publishers** (core stakeholders). Each publisher can have their own domain; content is filtered by the `Domain` the request originates from, showing only their data. All tenants share a single database -- this architecture will not change in the near term. Uses JWT/Session authentication.
- **portal_api** (Portal API): Mounted at `/portal/`. Internal admin portal for uploading, writing, updating, and managing content (full CRUD). Relies on a permission system; all users are expected to be internal company staff. Focus is on heavy lifting, not aesthetics. Uses JWT/Session authentication with role-based permissions.

Each app has its own `api/` directory with subdirectories per API surface (`internal/`, `public/`, `tenant/`, `portal/`), and each has its own tests.

- All queries in `api/tenant` should be filtered for publisher, preferably using `request.publisher_q()`

## Versioning
- **Tool**: `commitizen` (configured in `pyproject.toml` under `[tool.commitizen]`)
- **Version source**: `pyproject.toml` → `[project] version`
- **Tag format**: `v{version}` (e.g. `v0.2.0`)
- **Commit convention**: Conventional Commits enforced via pre-commit `commit-msg` hook and `commitlint.yml` CI workflow
  - `feat:` → bumps MINOR
  - `fix:` / `perf:` → bumps PATCH
  - `BREAKING CHANGE:` footer → bumps MAJOR
- **Bump command**: `cz bump --yes` (run by `version-bump.yml` on push to `main`)
- **Changelog**: auto-generated at `CHANGELOG.md` by `cz bump`; release-branch scaffold via `changelog.yml`
- **Sentry releases**: created automatically by `version-bump.yml` using org `itqan-3o`, project `cms-backend` / `cms-backend-staging`
- **Release branches**: named `release/v{version}` (e.g. `release/v1.0.0`); pushing to one triggers `changelog.yml`

## General
- **Dependency management**: Use `uv` (`uv sync` to install, `uv sync --group dev` for dev dependencies)
- **Linting**: Use `pre-commit install` to set up Git hooks. All PRs must pass linting checks to be merged
- Any function you define should be type hinted, you can exclude only API endpoints (decorated with `@router.`)
- All code should be Python 3.13 compatible and Django 5.2 compatible
- Always use `AwareDateTime` instead of `datetime` in `Schema`

## Architecture
- **WRITE operations** (POST/PUT/PATCH/DELETE): always go through Service → Repository → ORM. Services own validation, business rules, and transactions; Repositories own ORM access.
- **READ operations in `portal_api`** (GET list / GET retrieve in `apps/*/api/portal/`): query the Model directly inside the view function. Do **not** introduce a Service or Repository for portal reads. For retrieve endpoints, use `try/except Model.DoesNotExist` and raise `ItqanError(error_name="<resource>_not_found", status_code=404)` so the typed `404: NinjaErrorResponse[Literal["..._not_found"]]` OpenAPI contract is preserved. Filters from `FilterSchema` should be applied inline. The rationale: portal read paths have no business logic — keeping the queryset visible at the call site removes pass-through indirection.
- **READ operations in other API surfaces** (`tenant_api`, `developers_api`, `cms_api`/internal): continue to go through Service → Repository, since those surfaces typically need shared filtering (e.g. tenant scoping via `request.publisher_q()`) and other business logic.

The example below illustrates the WRITE-side Service/Repository contract.

- Always use Repositories for data access layer and Services for business logic layer (for WRITE operations and non-portal reads as described above)
The following code is for demonstration purposes:
A base repository so that it defines the contract for upcoming consumers therefore the code doesn't break.
```python
from abc import ABC, abstractmethod
from typing import Optional
import cache_service # Hypothetical external cache client

class BaseRecitationRepository(ABC):

    @abstractmethod
    def get_recitation(self, surah_number: int, reciter_name: str) -> Optional[dict]:
        """
        Retrieves the recitation data.
        Returns a dictionary (Entity), not a Django Model.
        """
        pass

    @abstractmethod
    def clear_cache(self, surah_number: int, reciter_name: str):
        """
        Clears the cache for this specific recitation.
        """
        pass
```
Here, the repository interacts with the data sources, gets data from the ORM, and caches it in Redis. Later,
we may use different technology for search, which will be a good fit.
```python
  class RecitationRepository(BaseRecitationRepository):

    def __init__(self):
        # We only interact with the Django ORM here
        self.model = QuranRecitation

    def get_recitation(self, surah_number, reciter_name):
        # 1. Construct Cache Key
        cache_key = f"quran:{surah_number}:{reciter_name}"

        # 2. Check Cache Service (Decoupled)
        cached_data = cache_service.get(cache_key)
        if cached_data:
            return cached_data

        # 3. Database Access via ORM
        # Note: The rest of the app doesn't know this is Django
        try:
            recitation = self.model.objects.get(
                surah_number=surah_number,
                reciter_name=reciter_name
            )
        except self.model.DoesNotExist:
            return None


        # 4. Cache It (Long TTL for recitations)
        cache_service.set(cache_key, entity, timeout=86400) # 1 Day
        return entity

    def clear_cache(self, surah_number, reciter_name):
        cache_key = f"quran:{surah_number}:{reciter_name}"
        cache_service.delete(cache_key)
```
The service will call the repository without bothering itself about the data internals or how it is retrieved,
 as the service should bother itself with business logic only.
```python
   class RecitationService:

    def __init__(self, repo: BaseRecitationRepository):
        self.repo = repo

    def get_audio_link(self, surah_number: int, reciter_name: str):
        """
        Business Logic: Check if recitation exists.
        """
        recitation = self.repo.get_recitation(surah_number, reciter_name)

        if not recitation or not recitation.get('is_active'):
            raise ValueError("Recitation not found or inactive.")

        return recitation['url']

    def update_recitation(self, surah_number: int, reciter_name: str, new_status: bool):
        """
        Business Logic: Update the DB and notify the user
        """
        # 1. Update Database (ORM)

       # Notify the user about the new update (Version)
```
